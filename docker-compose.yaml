version: '3.9'

services:
  fastapi:
    build: .
    container_name: chat-application
    ports:
      - "8000:8000"
    volumes:
      - .:/app
    restart: unless-stopped
    command: >
      uvicorn app.main:app
      --host 0.0.0.0
      --port 8000
      --reload